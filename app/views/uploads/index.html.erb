<h1>Listing uploads</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Photo</th>
    <th>Document</th>
    <th>Video</th>
    <th>Audio</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @uploads.each do |upload| %>
  <tr>
    <td><%= upload.name %></td>
    <td><%= image_tag upload.photo, :size => "150x150" %></td>
    <td><%= upload.document_file_name %></td>
    <td><%= upload.video_file_name %></td>
    <td><%= upload.audio_file_name %></td>
    <td><%= link_to 'Show', upload %></td>
    <td><%= link_to 'Edit', edit_upload_path(upload) %></td>
    <td><%= link_to 'Destroy', upload, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Upload', new_upload_path %>
